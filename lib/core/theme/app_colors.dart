import 'package:flutter/material.dart';

/// Color palette extracted from the auth_screen.html reference design.
class AppColors {
  AppColors._();

  // Primary
  static const Color primaryBlue = Color(0xFF14A9D5);
  static const Color primaryDark = Color(0xFF0D92BD);
  static const Color primaryLight = Color(0xFF44C6EB);

  // Text
  static const Color textDark = Color(0xFF334155);
  static const Color textLight = Color(0xFF94A3B8);

  // Feedback
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);

  // Surfaces
  static const Color white = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFE2E8F0);
  static const Color inputBorder = Color(0xFFE2E8F0);

  // Toast
  static const Color toastBg = Color(0xFF334155);
}
